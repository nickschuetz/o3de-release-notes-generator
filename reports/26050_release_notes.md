# 26.05.0 Release Notes

The 26.05.0 release brings significant enhancements to game development, with the most visible work landing in real-time rendering and authoring workflows alongside continued toolchain modernization and the first scaffolding for Apple Silicon support. The headline new feature is **Open Particle System**, a fresh native-VFX gem contributed in preview. It ships with its own runtime, an authoring editor, and example content covering three particle types — sprite, ribbon, and mesh — with the full complement of velocity, speed, and force modifiers a creator expects. Mesh-particle rendering is still labeled unsupported and emits a runtime warning at use time, so early adopters should treat that path as experimental. Two longstanding rendering correctness issues are also resolved this cycle: the **Diffuse Probe Grid** component, which until now considered only sky lighting when computing indirect light, now correctly bounces light from every direct light source and every emissive surface, materially changing how mid-frequency GI looks in real-world scenes; and the **Ray Tracing Debug** component gains two new visualization modes that surface the emissive and irradiance values being sent to the GPU, making material-driven lighting issues dramatically easier to diagnose. The Atom material library picks up a new **Unlit** material type and four new tone-mapping operators (the AgX family in four flavors plus Khronos PBR Neutral) alongside the existing ACES path, broadening the look-development options available to creators. **SkyAtmosphere** has been split out of Atom into its own gem — a clean separation that shrinks the shipping image for projects that don't use the feature, while existing levels continue to deserialize correctly thanks to a serializer migration that points old references at the new location.

On the authoring side, a new **Generic Asset Builder** lets Generic Asset Types — an existing engine concept that was previously self-managed — opt into the standard asset-building pipeline, gaining automatic dependency registration and binary conversion (with the runtime load-speed benefit that implies). The Editor itself becomes noticeably less ambiguous to use during long builds: a **progress overlay** now appears while Asset Processor is compiling critical assets, eliminating the "is it frozen?" perception that has caused users to force-quit otherwise-healthy sessions. Several targeted performance fixes round out the runtime story — a slowdown that affected projects calling `SetMaterialProperties` every frame, a crash that occurred when actor instances became invisible or had invalid meshes, and a physics regression caused by VHACD's convex-hull decomposition spawning far too many hulls, mitigated by re-budgeting the default cap to eight (configurable per-scenario).

Underneath the user-facing features, the toolchain takes a meaningful step forward. Builds now work cleanly on **Visual Studio 2026** and across the Clang 17–20 range, and a long-standing issue tracked as [o3de#19692](https://github.com/o3de/o3de/pull/19692) — which prevented the SDK variant from compiling against Clang 16 and newer — is finally resolved, unblocking a chunk of the contributor base that had been stuck on older compilers. The installer now ships with bundled **CMake 4.2.3** and **Ninja** binaries under `/CMake/runtime` and `/Ninja`; when a project is created or built through the Project Manager, those bundled tools take precedence over whatever is installed at the system level, eliminating one of the most common categories of build failure (mismatched system-CMake versions). Preliminary **Mac ARM64** scaffolding lands as a set of placeholder third-party dependency declarations ([o3de#19466](https://github.com/o3de/o3de/pull/19466)) supported by mac-arm64 binary updates across mcpp, OpenSSL, squish-ccr, astc-encoder, and ISPCTexComp ([o3de#19520](https://github.com/o3de/o3de/pull/19520)) — not a complete native build yet, but the first concrete step toward Apple Silicon as a first-class target.

Simulation and robotics, which will receive dedicated focus in the 26.10.0 release, still pick up substantial updates this cycle. **PhysX 4 is now formally deprecated** in favor of PhysX 5; a new `upgrade-physx-gem` tool is provided to migrate existing projects forward, and PhysX 5 itself gains an initial articulation offset and the ability to force rigid-body transforms without disabling the simulation step. The ROS 2 stack adds support for the **ROS 2 Lyrical Luth** LTS pre-release — a milestone the simulation community has been waiting on given Luth's LTS positioning — along with node-arguments wired through the engine registry and a broad sweep of subscription-handler, namespace, and rigid-body fixes. The **SimulationInterfaces** gem now spans the 1.4.0, 1.5.0, 1.6.0, and 2.1.0 versions of the standard and adds new APIs for batch entity spawn, reverse entity lookup (resolving a simulated entity by its root entity ID), state-change notifications, and explicit `INVALID_POSE` handling — driven by the robotics community's adoption of SimulationInterfaces as the cross-engine interop layer.

The features highlighted above were drawn from three sources: the manually-curated sig-release release-notes PR maintained by the release team, and the call-outs Jan Hanca (Robotec.ai) and Nick L (Hellaenergy) made directly in the sig-release Discord channel — both naming what their respective communities considered the cycle's highest-impact items. The remaining ~240 PRs in this release are listed in full under "Full List of Changes" below, grouped by SIG.

# Full list of changes

## SIG-Build
- Update 3P version and SHA256 hash for DirectXShaderCompilerDxc-1.8.2505.1. [o3de#19180](https://github.com/o3de/o3de/pull/19180)
- Removes the hundreds of lines of spam during configure relating to restricted objects. [o3de#19204](https://github.com/o3de/o3de/pull/19204)
- Python could not be found if \`$USERPROFILE\` was used because it was search for only in \`$HOME/.o3de/...\`. [o3de#19230](https://github.com/o3de/o3de/pull/19230)
- Update 3P version and SHA256 hash for DirectXShaderCompilerDxc-1.8.2505.1. [o3de#19241](https://github.com/o3de/o3de/pull/19241)
- Pin compiler dependancies for AR. [o3de#19276](https://github.com/o3de/o3de/pull/19276)
- When compiling development, I got an error from an warning, caused by the no usage of a variable. [o3de#19323](https://github.com/o3de/o3de/pull/19323)
- Suppresses  the following cmake warning for 3P Library open-mesh:. [o3de#19337](https://github.com/o3de/o3de/pull/19337)
- Allow to build all modules using latest version of MSVC with visual studio 2026. [o3de#19444](https://github.com/o3de/o3de/pull/19444)
- Visual studio was previously being disallowed in the project manager if the version was greater than the known working version. [o3de#19450](https://github.com/o3de/o3de/pull/19450)
- Update 3P version and SHA256 hash for DirectXShaderCompilerDxc-1.8.2505.1. [o3de#19456](https://github.com/o3de/o3de/pull/19456)
- This PR introduces a new macro \`AZ_COMPONENT_DECL_API\`, which is functionally identical to the existing \`AZ_COMPONENT_DECL\` macro but adds support for passing in an \`_API\` import/export macro. [o3de#19485](https://github.com/o3de/o3de/pull/19485)
- This PR improves CMake configuration errors for cases where executing AutoGen expansion rules fails. [o3de#19507](https://github.com/o3de/o3de/pull/19507)
- Fixes the follow error from the update to \[Entity Driven Startup and Hierarchical Activation Handling\](https://github.com/o3de/o3de/pull/19319) due to a missing include. [o3de#19508](https://github.com/o3de/o3de/pull/19508)
- Fixes a disk space reallocation issue due to changes in GHA disk layout for Debian Linux. GHA runners are now no longer using /mnt for temp folders and have resized the root disk for maximum space. This renders the resizing redundant and actually causes startup errors, as the resizing causes the ... [o3de#19531](https://github.com/o3de/o3de/pull/19531)
- This removes the windows shortcut to the Editor.exe. Launching the editor itself without providing a project path will popup a window saying "No project path was provided". The shortcut for the Project Manager from where the Editor can be launched by project still remains. [o3de#19542](https://github.com/o3de/o3de/pull/19542)
- The Quick Start mode of O3DE should not require Visual Studio or any compiler to be installed, only Ninja.  However, there was a programming error in the code which verified that ninja was present.  The code would correctly verify the presence of ninja, but instead of returning and allowing the b... [o3de#19548](https://github.com/o3de/o3de/pull/19548)
- This allows projects (for example MultiplayerSample) to have CMake options to select which gems to include, and then have those gems be included in the build, without having to modify the project.json file. [o3de#19555](https://github.com/o3de/o3de/pull/19555)
- Fix clang-cl based Windows builds — ### Configuration Changes. [o3de#19559](https://github.com/o3de/o3de/pull/19559)
- The CMake \`<LANG>_COMPILER_LAUNCHER\` option doesn't work with the Visual Studio or Xcode generators. There was an existing workaround implemented that fixed allowed VS solutions to use a launcher like ccache, but nothing was in place to handle Xcode. This change replaces the existing \`CompilerCac... [o3de#19560](https://github.com/o3de/o3de/pull/19560)
- CMake will use policy CMP0168 if it is available — This does NOT update the min requirement to 3.30, but takes advantage of the fetchcontent changes if it IS available. [o3de#19563](https://github.com/o3de/o3de/pull/19563)
- This changes the behavior on linux and windows to *prepend* the path to the embedded ninja and cmake runtimes as opposed to append them.  This means that if present they will prefer to use them.  We will need to update the runtimes we use to be the ones we know are good. [o3de#19564](https://github.com/o3de/o3de/pull/19564)
- Fix MSVC Debug Symbols with Compiler Launchers. [o3de#19568](https://github.com/o3de/o3de/pull/19568)
- Updates AR workflows to use CMake 4.2.3 to test against latest behavior as we move up the packaged CMake version described in https://github.com/o3de/o3de/issues/19574. [o3de#19578](https://github.com/o3de/o3de/pull/19578)
- This is a small cleanup in our CMake setup. [o3de#19583](https://github.com/o3de/o3de/pull/19583)
- Update bundled CMake to 4.2.3 and update build configurations. [o3de#19592](https://github.com/o3de/o3de/pull/19592)

## SIG-Content
- This PR changes the Warehouse Boxes prefabs hierarchy tree and updates the Storage Racks prefabs accordingly, to avoid confusing overrides in the Level prefab. [o3de-extras#1006](https://github.com/o3de/o3de-extras/pull/1006)
- This should add a checkbox on lod importer, which when turned on, will allow the importer to automatically generate the missing lods with meshoptimizer. [o3de#18711](https://github.com/o3de/o3de/pull/18711)
- We would like to use a different library for scene asset import yet still benefit from all offerings of the Scene API pipeline. This PR reviews the interface between Scene API and AssImp and fixes the abstraction where it was leaking or non-existent. It is an enabler for addition of different sce... [o3de#18931](https://github.com/o3de/o3de/pull/18931)
- Limit viewport resize events to prevent freezing the editor — Related to https://github.com/o3de/o3de/issues/16532 and \[talks on discord\](https://discord.com/channels/805939474655346758/855167846240485396/1384793102497087518+). [o3de#19033](https://github.com/o3de/o3de/pull/19033)
- Moved Project settings into the Editor Settings to be consistent. Closes #19139. [o3de#19143](https://github.com/o3de/o3de/pull/19143)
- This PR introduces a new toggle to invert the mouse wheel zoom direction, enhancing the user experience. It provides an optional setting to reverse the default zoom behavior. The feature is especially beneficial for professionals familiar with CAD tools. [o3de#19170](https://github.com/o3de/o3de/pull/19170)
- This PR resolves the issue #19065 limiting the user's pitch input to ±89 degrees. [o3de#19174](https://github.com/o3de/o3de/pull/19174)
- Currently when creating derived classes for reflected pointers in asset editor, abstract derived classes also show in the drop down dialog. This PR filters them out. [o3de#19181](https://github.com/o3de/o3de/pull/19181)
- fix-editor-go-to-position v2. [o3de#19185](https://github.com/o3de/o3de/pull/19185)
- EmotionFX: Ability for SimpleMotionComponent & AnimGraphComponent to exist on same entity — This PR is a revival of https://github.com/aws/lumberyard/pull/540, I revived it so that I can play an animation clip on the editor without having to disable or enable the simple motion component. [o3de#19192](https://github.com/o3de/o3de/pull/19192)
- This PR introduce a user friendly GUI tool to simplify component creation in O3DE projects. It is one-file-solution completly writen in Python, designed with beginners in mind, making it easy to modify, and extend without requiring a rebuild of the O3DE engine. [o3de#19203](https://github.com/o3de/o3de/pull/19203)
- This PR integrates the Component Creation GUI (introduced in \[PR #19203\](https://github.com/o3de/o3de/pull/19203)) directly into the O3DE Editor. Users can now launch the GUI from the Editor via the "New Component" menu option. [o3de#19225](https://github.com/o3de/o3de/pull/19225)
- Currently for animated meshes, the mesh-asset in the \`meshHandle\` held by the \`MeshFeatureProcessor\` is an asset that was created at runtime, and does not reference the actual asset produced by the \`AssetProcessor\`. [o3de#19240](https://github.com/o3de/o3de/pull/19240)
- Fixes the bug when using a nested prefab that has a long path. Any time you select the prefab the Inspector window force expands to display the entire path. After applying this fix you can shrink the Inspector and the window displays Prefab path as ".../simplified version of path.". [o3de#19254](https://github.com/o3de/o3de/pull/19254)
- The product file data (as in, the compiled asset) was always using asset references (\`AZ::Data::Asset<Material> etc\`) but the source json files were using string path names like \`"material" = "Atom/Examples/Blah.material"\`. [o3de#19255](https://github.com/o3de/o3de/pull/19255)
- Fix component selection display in Inspector window on Linux — ...and possibly some other issues that I didn't notice. [o3de#19258](https://github.com/o3de/o3de/pull/19258)
- This PR introduces a new tab widget dedicated to **Windows**-specific platform settings in the ProjectSettingsTool (\`Editor->File->Edit Platform Settings->Windows\`). [o3de#19262](https://github.com/o3de/o3de/pull/19262)
- Fixed all instances of error C3791: 'this' cannot be explicitly captured when the default capture mode is by copy (=). [o3de#19280](https://github.com/o3de/o3de/pull/19280)
- This PR allows procedural prefabs to correctly refresh if they contain any child Instances. Currently after when .procprefab is updated by AssetProcessor any child Instances do not have patches correctly applied. [o3de#19283](https://github.com/o3de/o3de/pull/19283)
- Simplify SceneAPI graph data. [o3de#19297](https://github.com/o3de/o3de/pull/19297)
- Let's call this PR a beta. I tried to move files into folder where they might fit the "best". There are plenty of other things to move but to not to get overwhelmed by just one PR (which it is already) we do this first. [o3de#19301](https://github.com/o3de/o3de/pull/19301)
- Moves our usage of OpenMesh to the local o3de mirror, and makes sure it doesn't install anything unnecessary.  Note that our mirror is identical to upstream, except for changing where it fetches a submodule from.  This will help when people are on restricted networks like university / company net... [o3de#19316](https://github.com/o3de/o3de/pull/19316)
- When closing the ScriptCanvas Application it would as an active process, this is because the shutdown call never happened. It's more complicated than that however. When Base::Destroy is called on an application it goes through a complex shutdown process that fails, this needs to be investigated. ... [o3de#19317](https://github.com/o3de/o3de/pull/19317)
- Entity Driven Startup and Hierarchical Activation Handling. [o3de#19319](https://github.com/o3de/o3de/pull/19319)
- This PR tries to replace the custom job system of the particle system by O3DE own job system. [o3de#19320](https://github.com/o3de/o3de/pull/19320)
- The arguments to QProcess::startDetached were being passed in incorrectly via string interpolation which can cause some string escape issues and cause it to fail to find ffmpeg. The idiomatic way of passing in the arguments list is used elsewhere in the engine so this fix brings it in line. [o3de#19324](https://github.com/o3de/o3de/pull/19324)
- The JointSelection dialog seem not to expand correctly within the first column (using QTreeView). [o3de#19336](https://github.com/o3de/o3de/pull/19336)
- Replaces Vector2, Vector3, Vector4, Transform, Quaternion aliases with AZ::Vector ..... and gets rid of some header files. It does not change any functionality. [o3de#19360](https://github.com/o3de/o3de/pull/19360)
- Fix #19348 so that the shortcuts for the Script Canvas works with the standard and the standalone version. [o3de#19364](https://github.com/o3de/o3de/pull/19364)
- This is actually a fix for the Qt6 branch where the EMFX shows a black screen. I do not know why OpenGL was used for those widgets but this shouldn't be used and its not needed in my point of view. The reason I am targeting the 'development' branch is, that I think those OpenGL widgets shouldn't ... [o3de#19366](https://github.com/o3de/o3de/pull/19366)
- Removed m_pDefaultButton, not used, breaks build when warnings are treated as errors. [o3de#19370](https://github.com/o3de/o3de/pull/19370)
- PropertyHandlerDirectory is only used in one place within O3DE: the LyShine Gem's UiImageSequenceComponent.h. [o3de#19375](https://github.com/o3de/o3de/pull/19375)
- As the title says. This PR fixes some reflection issues and does some cleanup. [o3de#19376](https://github.com/o3de/o3de/pull/19376)
- AssetBundler did not actually save paths you specify in the GUI, since the intention there was that the gui is for constructing rule files that have blanks for you to fill in later, from the command line.  So for example, if you made a rule that generated the delta between 2 different asset lists... [o3de#19419](https://github.com/o3de/o3de/pull/19419)
- This PR adds the \`const\` modifier to the function \`AZ::SceneAPI::Containers::SceneGraph::Find(const Name& name)\`, since the modifier is also present in the other Find functions with different parameters and this functions does not modify the SceneGraph class. [o3de#19421](https://github.com/o3de/o3de/pull/19421)
- Adjust PrefabDomUtils to distinguish TransformComponent and GradientEditorTransformComponent. [o3de#19422](https://github.com/o3de/o3de/pull/19422)
- This PR simply removes some dead code from Editor codebase since it is not referenced by anyone. [o3de#19438](https://github.com/o3de/o3de/pull/19438)
- This PR fixed a coordinate system error when importing animation-only FBX files which was previously fixed by #19061 but was reintroduced by #18931. [o3de#19440](https://github.com/o3de/o3de/pull/19440)
- When importing a mesh which contains any kind of keyframes, it can under certain conditions be processed wrong (see #19431): This can lead to the a scene graph where a mesh node is the child of a bone node, and both nodes have a transform child node with the same parameters, which leads to the sa... [o3de#19478](https://github.com/o3de/o3de/pull/19478)
- Deprecation of Cry_Color (\`ColorF\`, \`ColorB\`). [o3de#19505](https://github.com/o3de/o3de/pull/19505)
- Adds a progress overlay when editor is processing critical assets. [o3de#19513](https://github.com/o3de/o3de/pull/19513)
- This makes it so that levels and prefabs that have previously been created with the SkyAtmosphere gem will continue to find the component in their prefab files and levels, despite it having changed its name. [o3de#19547](https://github.com/o3de/o3de/pull/19547)
- \\[Editor\\] Fix many instances of project path args not being wrapped in quotes — This PR improves support for engine users who may have spaces in their project pathnames. [o3de#19551](https://github.com/o3de/o3de/pull/19551)
- Original ClassWizard is not appearing in \`/Tools/\` on "install" type build package output. [o3de#19561](https://github.com/o3de/o3de/pull/19561)
- 1. Fixes a bug where the "auto load behavior" is blown away when you assign an asset in the gui causing all GUI assigned assets to become QueueLoad. 2. Adds a "generic asset builder" to the Asset Processor.   We already had GenericAssets, this just adds a builder for them if the user opts in to d... [o3de#19572](https://github.com/o3de/o3de/pull/19572)

## SIG-Core
- 2510.1 point release: update versions of gems; update repo.json. [o3de-extras#1013](https://github.com/o3de/o3de-extras/pull/1013)
- PR replaces AZStd::string with c_str to make it compatible with ROS 2. [o3de-extras#1027](https://github.com/o3de/o3de-extras/pull/1027)
- Solves missing reflection for PidMotorControllerComponent. [o3de-extras#1036](https://github.com/o3de/o3de-extras/pull/1036)
- This change makes restricted objects completely optional for ALL objects, including the engine which previously required a restricted object. Now if the object doesn't have a restricted or if restricted object if not present will just default to the <object_path>/restricted. This is fine as long ... [o3de#19080](https://github.com/o3de/o3de/pull/19080)
- This PR is a rebase of #9632 (Streamer throughput optimizations) to the latest dev branch. [o3de#19156](https://github.com/o3de/o3de/pull/19156)
- I was trying to benchmark the streamer stuff to compare it for the new PR that improves it, and found these benchmarks to be horribly broken and full of memory problems. [o3de#19209](https://github.com/o3de/o3de/pull/19209)
- This PR simply add operator\[\] to Vector{2,3,4} and Matrix{3,4}x{3,4} types so that matrix elements can be accessed through something like matrix\[2\]\[3\] in addition to GetElement/SetElement. [o3de#19228](https://github.com/o3de/o3de/pull/19228)
- Currently, when debugging on Linux the cursor is hidden which makes it hard to use any IDE while debugging the engine. This PR will prevent the engine to hide the cursor when the debugger is present. Not sure if there is any other trick on Linux I am not aware of to prevent this , if someone know... [o3de#19278](https://github.com/o3de/o3de/pull/19278)
- Adds additional unit tests for AZStd::copy_backward — One github actions machine failed one time with this test, in a way I can't figure out. [o3de#19284](https://github.com/o3de/o3de/pull/19284)
- Unify fast math across all platforms. [o3de#19303](https://github.com/o3de/o3de/pull/19303)
- InterlockedIncrement and InterlockedDecrement signatures — Arch-Linux, Clang 21.1.5, treats the return of "const volatile" as error/warning. const volatile is _deprecated. [o3de#19359](https://github.com/o3de/o3de/pull/19359)
- Adds a missing override in AzCore/RTTI/BehaviorContext.h. [o3de#19389](https://github.com/o3de/o3de/pull/19389)
- Extend the Look At component's capabilities. [o3de#19441](https://github.com/o3de/o3de/pull/19441)
- Addresses multiple macOS build issues, with a primary focus on restoring support for generic engine builds. (i.e., when the engine is built standalone, without an accompanying project.). [o3de#19445](https://github.com/o3de/o3de/pull/19445)
- Update 3P version and SHA256 hash for zlib-1.3.1. [o3de#19471](https://github.com/o3de/o3de/pull/19471)
- Deprecation of Cry_Matrix44 (\`Matrix44\`). [o3de#19472](https://github.com/o3de/o3de/pull/19472)
- Update 3P version and SHA256 hash for expat-2.7.3. [o3de#19474](https://github.com/o3de/o3de/pull/19474)
- CryEngine used to have its own particle system, long removed.  This removes the leftover test files and references to it in code. [o3de#19475](https://github.com/o3de/o3de/pull/19475)
- Update 3P version and SHA256 hash for OpenEXR-3.4.4. [o3de#19480](https://github.com/o3de/o3de/pull/19480)
- This deprecates all the CRY_ASSERT* macros which were forwarding macros to the AZ_Assert macros anyways. This preserves the original \`assert\` macro due to the large number of uses in the CRY_* legacy code for now. [o3de#19482](https://github.com/o3de/o3de/pull/19482)
- Cry_Quat deprecation. [o3de#19484](https://github.com/o3de/o3de/pull/19484)
- Update 3P version and SHA256 hash for tiff-4.2.0.15. [o3de#19487](https://github.com/o3de/o3de/pull/19487)
- Update 3P version and SHA256 hash for freetype-2.11.1. [o3de#19488](https://github.com/o3de/o3de/pull/19488)
- Update 3P version and SHA256 hash for DirectXShaderCompilerDxc-1.8.2505.1. [o3de#19489](https://github.com/o3de/o3de/pull/19489)
- Cry_Matrix34 deprecation. [o3de#19492](https://github.com/o3de/o3de/pull/19492)
- Update Cry Legacy to use \`AZStd::this_thread::get_id()\` instead of \`CryGetCurrentThread()\`. [o3de#19501](https://github.com/o3de/o3de/pull/19501)
- Cry_Matrix33 deprecation. [o3de#19502](https://github.com/o3de/o3de/pull/19502)
- Cry Common Cleanup Chores. [o3de#19514](https://github.com/o3de/o3de/pull/19514)
- Update 3P version and SHA256 hash for mcpp-2.7.2_az.2-rev1-mac-arm64, OpenSSL-1.1.1w-rev1-mac-arm64, squish-ccr-deb557d-rev1-mac-arm64, astc-encoder-3.2-rev5-mac-arm64, ISPCTexComp-36b80aa-rev1-mac-arm64. [o3de#19520](https://github.com/o3de/o3de/pull/19520)
- This PR changes the definition of named colors in the \`AZ::Colors\` namespace to use u8-RGB values instead of float values in the range 0..1. This was made possible by the new constructors of \`AZ::Color\` in #19505. This does not really change anything, it just makes the declarations more similar t... [o3de#19522](https://github.com/o3de/o3de/pull/19522)

## SIG-Graphics-Audio
- Passes used in \`ROS2CameraSensorComponent\` were not moved from \`ROS2\` Gem to \`ROS2Sensors\` Gem when the code was moved. It worked correctly, as \`ROS2Sensors\` depends on \`ROS2\`. However, it makes no sense to keep the assets in the wrong place. [o3de-extras#991](https://github.com/o3de/o3de-extras/pull/991)
- With this change user can pass ROS 2 node arguments via O3DE Registry. It might be very useful for remapping and other applications. [o3de-extras#1014](https://github.com/o3de/o3de-extras/pull/1014)
- This PR introduces alternate frame rendering (AFR) functionality to the engine by latching on to a \`RenderPipeline\`, capturing all passes that are not already scheduled to a fixed device (as well as the \`CopyToSwapchain\` pass which always runs on the default display device), alternating them betw... [o3de#19060](https://github.com/o3de/o3de/pull/19060)
- This PR changes the \`Atom_Feature_Common.Public\` target from a static to a shared library. This is similar to the conversion of the Atom_RHI targets to shared libraries in \[PR19154\](https://github.com/o3de/o3de/pull/19154), but in this case it is a much smaller changeset since most classes in thi... [o3de#19166](https://github.com/o3de/o3de/pull/19166)
- This PR removes the default constructor of the \`AZ::RHI::GeometryView\` class. The one-argument constructor was made explicit in \[PR19067\](https://github.com/o3de/o3de/pull/19067) to disallow assigning a \`MultiDevice::DeviceMask\` to the GeometryView, which is semantically ambiguous. The default co... [o3de#19176](https://github.com/o3de/o3de/pull/19176)
- This PR removes usages of the \`AZ_FORCE_CPU_GPU_INSYNC\` macro from public Atom-RHI headers and replaces it with the constexpr \`AZ::RHI::ForceCpuGpuInSync\` boolean variable, which is no longer used in RHI header files. [o3de#19179](https://github.com/o3de/o3de/pull/19179)
- The variable \`bool found\` in AFRFeatureProcessor.cpp is defined, set and then later only read inside a \`AZ_Assert\` call, which leads to a compile error in release build. [o3de#19188](https://github.com/o3de/o3de/pull/19188)
- Remove any structs from disabled extensions in pNext chain in Vulkan. [o3de#19190](https://github.com/o3de/o3de/pull/19190)
- 1. Adds a function to 3rdparty.cmake to handle 3rd party fetchcontent libraries, following DRY principle 2. Updates miniaudio to latest stable release. 3. Ensures that the vorbis plugin is installed and running 4. Ensures that the node plugins for miniaudio are included in the install. [o3de#19195](https://github.com/o3de/o3de/pull/19195)
- After #18237 we are no longer able to use render passes without output attachments (i.e. raster passes that only write to sideband buffers) following an assert inside \`GetRenderAttachmentConfiguration()\`.  This was supposedly explicitly supported before and seems to be caused by an unneeded early... [o3de#19196](https://github.com/o3de/o3de/pull/19196)
- This PR sets the \`emissive.enable\` material property to true when an emissive color is set for the material. Currently, emissive properties are set for the material, but ignored at runtime unless emissive is enabled manually (eg. in the material instance editor for a material slot). [o3de#19206](https://github.com/o3de/o3de/pull/19206)
- FrameGraphExecuter: Limit commandListCount to estimatedItemCount. [o3de#19207](https://github.com/o3de/o3de/pull/19207)
- Fixes the \`BufferPool::MapBuffer\` and \`UnmapBuffer\` functions for buffers that are not on all devices the \`BufferPool\` is on. [o3de#19210](https://github.com/o3de/o3de/pull/19210)
- This PR adds the \`const\` qualifier to the \`GetBufferView\` and \`GetImageView\` (single and multi-device) functions of \`RHI::Buffer\` and \`RHI::Image\`, which makes const-correctness in downstream usage of these functions easier (Eg. usages of \`const_cast\` in \[\`MeshFeatureProcessor\`\](https://github.co... [o3de#19211](https://github.com/o3de/o3de/pull/19211)
- Introduce OpenParticleSystem — This PR contributes an open particle system to the O3DE. It is a standalone Gem. It contains a particle runtime, a particle editor, particle asset and its builder. [o3de#19213](https://github.com/o3de/o3de/pull/19213)
- In the Vulkan RHI implementation there is the enum \[\`OptionalDeviceExtension\`\](https://github.com/o3de/o3de/blob/3f129b283c9516be2c4f9b5786edf632fda1caa6/Gems/Atom/RHI/Vulkan/Code/Source/RHI/PhysicalDevice.h#L44), and a separate \[\`OptionalDeviceExtensionNames\`\](https://github.com/o3de/o3de/blob/3... [o3de#19215](https://github.com/o3de/o3de/pull/19215)
- This PR adds basic support for using Cluster-level acceleration structures (CLAS) with the Vulkan RHI. [o3de#19218](https://github.com/o3de/o3de/pull/19218)
- Meshlets gem has been neglected for a while and has been uncompilable due to the change of Atom API and the absence of meshoptimizer library in O3DE. This PR fixes the outstanding build errors in the gem. [o3de#19222](https://github.com/o3de/o3de/pull/19222)
- This PR removes some function declarations in Atom which have no implementation. These occurrences were found by the IDEs code inspection, which showed that the function \`RayTracingAccelerationStructurePass::CreateRayTracingAccelerationStructureBuildFlags\` was not implemented, so I wondered if th... [o3de#19223](https://github.com/o3de/o3de/pull/19223)
- Deferred Fullscreen Draw Items — This PR replaces \[PR 18740\](https://github.com/o3de/o3de/pull/18740), since that PR had several impactful changes that were merged in separate PRs since. [o3de#19231](https://github.com/o3de/o3de/pull/19231)
- The miniaudio target was missing a dependnecy on vorbisenc - this causes it not to build it, despite it wanting to deploy / copy it. [o3de#19234](https://github.com/o3de/o3de/pull/19234)
- This PR makes the first parameter of the \`AZ::Render::BufferViewIndexAndOffset/IndexBufferViewIndexAndOffset\` creation functions \`const\`, such that they can be used with temporary and const arguments. [o3de#19245](https://github.com/o3de/o3de/pull/19245)
- Replace OpenParticleSystem SimuCore math with AzCore math. [o3de#19248](https://github.com/o3de/o3de/pull/19248)
- When calling \`MeshFeatureProcessorInterface::UpdateMeshInfoEntry\`, the return value of the callback function should indicate whether the underlying GPU buffer needs to be updated, but currently the \`MeshInfoManager::m_meshInfoNeedsUpdate\` field is always set to the return value of the most-recent... [o3de#19249](https://github.com/o3de/o3de/pull/19249)
- This PR removes some no longer necessary \`const_cast\` expressions in the Atom gem, some of which were made possible due to \[PR19211\](https://github.com/o3de/o3de/pull/19211), others for unrelated reasons. [o3de#19251](https://github.com/o3de/o3de/pull/19251)
- Revert "Delay viewport resize to prevent freezing the editor on contnuous resize (#19033)" — This reverts commit cd4745ca350727b5c8982fc51bf469eac94a1cee. [o3de#19261](https://github.com/o3de/o3de/pull/19261)
- \`\`\` Jobs can only depend on the "common" platform or other jobs for the same platform. This is a code error, not a problem with the assets - please modify the builder to emit job\` dependencies correctly. Source Job: (platform "server", job Key: "Particle Builder") Depends on: (platform "pc", job ... [o3de#19264](https://github.com/o3de/o3de/pull/19264)
- Fix compile issues, also the way particles are created, opened and cleanups — This PR has multiple changes. [o3de#19266](https://github.com/o3de/o3de/pull/19266)
- This PR fixes behaviour when setting mesh asset using \`AzToolsFramework::Components::EditorComponentBase\` and \`SetPrimaryAsset\`. Previous asset assignment missed assigning asset hint what caused unwanted overrides in level.prefab. [o3de#19287](https://github.com/o3de/o3de/pull/19287)
- This PR fixes a crash when enabling ray tracing for a debug-OBB shape. This was likely an oversight from #19123, which introduced new requirements for users of the ray tracing APIs, and added the same code for acquiring the MeshFeatureProcessor when generating debug-sphere ray tracing data, but n... [o3de#19298](https://github.com/o3de/o3de/pull/19298)
- Feature: Additional Tonemapping Operators. [o3de#19312](https://github.com/o3de/o3de/pull/19312)
- This PR changes the AssetProcessor behavior such that missing includes are treated as job failure. Before that, a missing include lead to an error in the event log, but shader processing continued, and only failed if the missing include leads to further compile errors. This behavior does not seem... [o3de#19315](https://github.com/o3de/o3de/pull/19315)
- Fixes a bug where particles would discard mesh or skeleton mesh info — The code is simply wrong, it was only saving fields if empty, instead of if not empty. [o3de#19328](https://github.com/o3de/o3de/pull/19328)
- Fixes so that the module can be found also client and server builds. [o3de#19338](https://github.com/o3de/o3de/pull/19338)
- \\[OpenParticleSystem\\] Small fixes and editor improvements — This PR does the following:. [o3de#19342](https://github.com/o3de/o3de/pull/19342)
- Improve some Atom error messages. [o3de#19344](https://github.com/o3de/o3de/pull/19344)
- Fixes the preview feature of the Atom pass viewer for depth images. [o3de#19367](https://github.com/o3de/o3de/pull/19367)
- This adds 2 new preview render modes to the existing ray trace debug component:  Emissive Color, irradiance color. [o3de#19387](https://github.com/o3de/o3de/pull/19387)
- Updates the shaders and fixes a missing SRG (SceneSRG) for them, since the Raytracing SRG was folded into the Scene SRG. fixes https://github.com/o3de/o3de/issues/19198. [o3de#19388](https://github.com/o3de/o3de/pull/19388)
- Backslash (\\) is used by Windows as path separator but also used by unicode for escape sequences. Since #19315, shader include path expressed in Windows style causes error such as \`error: Illegal UCN sequence\`. This PR fixes the errors by converting the path to posix style. [o3de#19393](https://github.com/o3de/o3de/pull/19393)
- Fix FFont::DrawScreenAlignedText3d #18562. [o3de#19414](https://github.com/o3de/o3de/pull/19414)
- Adds missing assets to the default seed lists for Asset Bundling / Release shipping — A few shaders had been added or changed, since last the seedlist was made, and the seedlist has not been updated to compensate. [o3de#19418](https://github.com/o3de/o3de/pull/19418)
- Add Unlit material type to Atom Gem. [o3de#19427](https://github.com/o3de/o3de/pull/19427)
- 1.  Depending on random memory allocation, it was possible for the particle data pool to choose an alignment that was not SIMD aligned.  This would lead to mystery crashes on some runs and not others. Fixed by adding a custom allocator to its vector, which allocates always at least 16 byte aligne... [o3de#19433](https://github.com/o3de/o3de/pull/19433)
- Fixes a build break on macOS with Xcode 26.1.1 when compiling the Meshlets gem with Xcode. [o3de#19437](https://github.com/o3de/o3de/pull/19437)
- Image Based Lights: Expose GetExposure() and SetExposure(...) methods to behavior context for use in scripting. [o3de#19476](https://github.com/o3de/o3de/pull/19476)
- This PR extracts sky atmosphere feature from Atom into its own gem as a clean-up of Atom code. [o3de#19479](https://github.com/o3de/o3de/pull/19479)
- This PR is a follow up of https://github.com/o3de/o3de/pull/19479. It removes hard-coded SkyAtmosphereParentPass from Atom's OpaqueParent pass and inserts one after SkyBox pass only when SkyAtmosphere Gem is enabled. [o3de#19490](https://github.com/o3de/o3de/pull/19490)
- This is the last batch of changes that separates SkyAtmosphere from Atom. This PR moves the SkyAtmosphere-related pass templates into SkyAtmosphere gem. [o3de#19494](https://github.com/o3de/o3de/pull/19494)
- This PR enable anisotropy in default samplers. It help alleviate moire pattern in render. [o3de#19524](https://github.com/o3de/o3de/pull/19524)
- Fixes an iOS build issue in the MiniAudio gem by forcing \`miniaudio.c\` to compile as Objective-C on Apple platforms. [o3de#19584](https://github.com/o3de/o3de/pull/19584)
- Fixes Apple builds for the MiniAudio (again). [o3de#19586](https://github.com/o3de/o3de/pull/19586)

## SIG-Network
- This PR removes the file \`AzNetworking_input_files.txt\` from the source tree, which was erroneously added in #19280. This file is generated by a custom Cmake rule as part of the build, so it should not be checked in to git. Also, this file should normally be in the build folder (\`${CMAKE_CURRENT_... [o3de#19285](https://github.com/o3de/o3de/pull/19285)

## SIG-Platform
- Updates the CMake and Linux defines for OpenXR. Currently we check for either X11 or Wayland but with PR https://github.com/o3de/o3de/pull/19477 its now supported to have both. [o3de-extras#1019](https://github.com/o3de/o3de-extras/pull/1019)
- Remove Interlocked functions and mutex from LinuxSpecific.h — Revisits const volatile that cannot be returned, volatile cannot be returned either. [o3de#19369](https://github.com/o3de/o3de/pull/19369)
- Adding the following preliminary files for the 3P cmake files for arm64 based on the x86_64 equivalent. [o3de#19466](https://github.com/o3de/o3de/pull/19466)

## SIG-Simulation
- Fixes bug with finger gripper not seeing all joints. Fixes https://github.com/o3de/o3de-extras/issues/978. [o3de-extras#981](https://github.com/o3de/o3de-extras/pull/981)
- This PR adds a check for a root articulation (it was recently added to the engine, see https://github.com/o3de/o3de/pull/19227) in \`JointManipulationComponent\`. Previously. the code was looking for a free axis of a joint (root articulation has none), which resulted in the same early return. The o... [o3de-extras#982](https://github.com/o3de/o3de-extras/pull/982)
- ROS 2 project templates pull Gems using dependencies. It uses \`ROS2SampleRobots\` Gem, which requires \`ROS2Controllers\` and \`ROS2Sensors\`, which are not pulled into the project due to a problem with O3DE: https://github.com/o3de/o3de/issues/19199. [o3de-extras#984](https://github.com/o3de/o3de-extras/pull/984)
- Ackermann's \`WheelController\` has a setting called \`SteeringScale\` which is supposed to modify direction (and scaling) of steering force. It allows changing steering behavior - for example vehicles with rear-wheel steering (like forklifts) require opposite steering directions. However, the \`Steer... [o3de-extras#985](https://github.com/o3de/o3de-extras/pull/985)
- The \`JointsTrajectoryComponent\` would not check if the goal position was reached, and would report success based solely on the time elapsed from accepting the trajectory. This results in reporting success before the movement stops, thus making it impossible to chain multiple actions (involving ot... [o3de-extras#987](https://github.com/o3de/o3de-extras/pull/987)
- This PR fixes a limit to PID controllers. [o3de-extras#988](https://github.com/o3de/o3de-extras/pull/988)
- The assets were referenced using URI information from SDFormat/URDF file. This fails when parsing worlds - there might be multiple different _models_ having the same URI name (e.g. \`base_visual\`). If this happens, all names collide. Moreover, missing information about the source _model_ causes fa... [o3de-extras#989](https://github.com/o3de/o3de-extras/pull/989)
- PR #988 changed the PID implementation to allow the negative values. The tests were not updated and they started failing. This is fixed now. [o3de-extras#990](https://github.com/o3de/o3de-extras/pull/990)
- It removes assert from NamespaceComputation method, which traverses all entities from the leaf to the root. The root here can be a level entity, which has no transform. Hence, the assert always triggers. [o3de-extras#994](https://github.com/o3de/o3de-extras/pull/994)
- \`defaultSceneHandle\`  variable is only used in \`AZ_Assert\`, which means it is unused when build in release. It triggers errors in some compilers configurations. [o3de-extras#1000](https://github.com/o3de/o3de-extras/pull/1000)
- frame id was hard coded. frame component was required. Loading variable frame id now from component like is done in other sensors. [o3de-extras#1005](https://github.com/o3de/o3de-extras/pull/1005)
- This PR moves ROS 2 handlers base classes from \`Simulation Interfaces\` Gem to \`ROS2\` to allow for better reusability of the code without need to use \`simulation_interfaces\` standard. [o3de-extras#1007](https://github.com/o3de/o3de-extras/pull/1007)
- It makes settings registry key for ros2 handlers consistent with the rest of the ROS2 Gem. [o3de-extras#1009](https://github.com/o3de/o3de-extras/pull/1009)
- This PR fixes simulation interfaces LevelManager to correctly handle loading levels via ImGui. Original solution wasn't restoring internal state in case of request fail. [o3de-extras#1020](https://github.com/o3de/o3de-extras/pull/1020)
- Send setpoint to articulation only when it is needed. [o3de-extras#1023](https://github.com/o3de/o3de-extras/pull/1023)
- Two main issues were fixed in this PR: 1. \`JointsPositionComponent\` relies on \`JointsManipulationService\`, which was not marked before 2. In all places we use joint names together with the namespace, see e.g. https://github.com/o3de/o3de-extras/blob/36d176cf6e1a3cda07eae479a2187470accf89ee/Gems/R... [o3de-extras#1026](https://github.com/o3de/o3de-extras/pull/1026)
- PR adds reverse lookup to simulation interfaces to retrieve simulated entity name based on root entity Id. [o3de-extras#1028](https://github.com/o3de/o3de-extras/pull/1028)
- PR removes warning from GetEntities. User should not be forced to select specific shape in c++ API. Compatibility with standard is checked in ROS 2 layer anyway. [o3de-extras#1029](https://github.com/o3de/o3de-extras/pull/1029)
- PR adds missing check for static rigid bodies colliders. It makes it possible to query static rigid bodies via GetEntities with bounds filter. [o3de-extras#1030](https://github.com/o3de/o3de-extras/pull/1030)
- PR adds notification triggered on simulation state change. It also changes all \`SimulationManagerNotifications\` from pure virtual to virtual with empty implementation to not force users  to define both callbacks if they don't need it. [o3de-extras#1031](https://github.com/o3de/o3de-extras/pull/1031)
- Add feature to batch spawn entities using \`SimulationEntityManagerRequests\` EBus. [o3de-extras#1032](https://github.com/o3de/o3de-extras/pull/1032)
- PR moves path/uri utilities to public API target. [o3de-extras#1033](https://github.com/o3de/o3de-extras/pull/1033)
- Robotic templates fixes. [o3de-extras#1041](https://github.com/o3de/o3de-extras/pull/1041)
- Development branch of ROS 2 (Rolling) marked headers with \`.h\` extension as deprecated, triggering warnings that stop compilation (we treat warnings as errors). This would cause problems when building ROS2 Gem in O3DE 2605 with ROS 2 Lyrical Luth (to be released in May). [o3de-extras#1042](https://github.com/o3de/o3de-extras/pull/1042)
- Fix \`ROS2FrameComponent\` tests failing after #1037 due to numeric entity names:. [o3de-extras#1043](https://github.com/o3de/o3de-extras/pull/1043)
- This PR replaces the use of gazebo_msgs for spawning robots with simulation_interfaces. The Nav2 configuration has also been adjusted slightly to address the issue of the AMCL losing the robot's place on the map during movement. [o3de-extras#1050](https://github.com/o3de/o3de-extras/pull/1050)
- This PR allows users to adjust the playback speed for motions through the emfx importer (also allows the animation to be reversed). [o3de#19173](https://github.com/o3de/o3de/pull/19173)
- Set initial position in hinge articulation within limits; Add IsRootArticulation to bus. [o3de#19227](https://github.com/o3de/o3de/pull/19227)
- Set initial position in hinge articulation within limits; Add IsRootArticulation to bus — Cherry-pick #19227 from \`stabilization\` to \`development\`. [o3de#19263](https://github.com/o3de/o3de/pull/19263)
- PhysX4 Deprecation Notice Preperation. [o3de#19582](https://github.com/o3de/o3de/pull/19582)
- Additional updates for the physx gem migration script. [o3de#19588](https://github.com/o3de/o3de/pull/19588)
- The articulation was created without possibility to apply rotation. [o3de#19594](https://github.com/o3de/o3de/pull/19594)

